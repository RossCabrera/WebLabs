<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Secrets API</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">

  <div class="container py-5">

    <div class="text-center mb-4">
      <h1 class="fw-bold">🔐 Secrets API Demo</h1>
      <p class="text-muted">Authentication Examples</p>
    </div>

    <!-- Buttons -->
    <div class="d-flex flex-wrap justify-content-center gap-3 mb-5">
      <a href="/noAuth" class="btn btn-primary btn-lg">No Auth</a>
      <a href="/basicAuth" class="btn btn-success btn-lg">Basic Auth</a>
      <a href="/apiKey" class="btn btn-warning btn-lg">API Key</a>
      <a href="/bearerToken" class="btn btn-danger btn-lg">Bearer Token</a>
    </div>

    <!-- Response Card -->
    <div class="card shadow-lg border-0">
      <div class="card-body">
        <h5 class="card-title mb-3">API Response</h5>

        <% if (content) { %>
          <pre class="bg-dark text-light p-3 rounded">
            <%= content %>
          </pre>
          <% } else { %>
            <p class="text-muted">Click a button to fetch data.</p>
            <% } %>

      </div>
    </div>

  </div>

</body>

</html>