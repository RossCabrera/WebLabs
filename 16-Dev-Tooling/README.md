# 16 — Dev Tooling

Reference notes for the core development toolchain.

## Topics
- `mise` — runtime version manager (Python, Node, Bun per project)
- `uv` — Python package and venv manager
- Package managers overview (bun, pnpm, npm, pip — when to use what)
- Workflow patterns and best practices

## Structure
```
16-Dev-Tooling/
└── notes/        ← one file per tool
```
