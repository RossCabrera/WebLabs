# 🎞️ 04 — Gifs App

Requires a free Giphy API key. Create a `.env` file:

```bash
VITE_GIPHY_API_KEY=your_key_here
```

Get a key at `https://developers.giphy.com`. Then run:

```bash
bun install
bun run dev
```
